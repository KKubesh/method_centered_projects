import React from 'react';

const Header = ({ title }) => (
  <div>
    <div className="center-item">
      <h1>{ title }</h1>
    </div>
    <div className="bannerback">
    </div>
  </div>
);

export default Header;
